// styles
import generalStyles from '../GeneralStyles/GeneralStyles'

const style = {}

style.clear = {
  clear: 'both',
}

style.keyContainer = {
  margin: '0 auto',
  maxWidth: 300,
  width: '100%',
}

style.key = {
  fontSize: 12,
  float: 'left',
  textAlign: 'left',
}

style.rsvpBarContainer = {
  marginTop: 50,
  width: '100%',
}

export default style
